# Team Member 4 — Final UX & Reliability Layer

Adds an isolated final-polish layer after Team Member 3.

## Added
- `app/not-found.tsx` — branded 404 experience
- `app/error.tsx` — retryable global error experience
- `components/common/EmptyState.tsx` — reusable empty-state component
- `components/common/PageLoader.tsx` — reusable loading component

## Database
No Supabase migrations, seeds, API changes, or environment-variable changes.

## Suggested commit
`feat: add final ux error states and loading components`
